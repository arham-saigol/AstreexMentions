{
  "name": "@astreex/security",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@astreex/config": "workspace:*",
    "@types/node": "^24.0.0",
    "eslint": "^9.0.0",
    "typescript": "^6.0.0",
    "vitest": "^4.0.0"
  }
}
